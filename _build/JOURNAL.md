# Journal de build — La Fusée v2

Une ligne par décision. Format : `AAAA-MM-JJ — décision`.

- 2026-07-05 — Démarrage Phase 0. Repo vide confirmé, Node 22.22, Postgres 16 local démarré (db `lafusee` + `lafusee_test`), branche `claude/la-fusee-v2-rebuild-9omp5v`.
- 2026-07-05 — `_reference/` ABSENT du repo : seeds canon UPgraders/La Fusée et assets de marque (logos, fonts) seront des placeholders propres rédigés depuis le cahier ; fonts tentées depuis Fontshare (licence libre), sinon fallback système. À remplacer par l'opérateur si le dump v1 devient disponible.
- 2026-07-05 — Couche API : Server Actions + Route Handlers (pas de tRPC) — les Server Components lisent les services directement ; les route handlers ne servent que les vrais contrats HTTP (webhooks, SSE, crons, MCP, intake). Moins de couches pour la même sécurité de types.
- 2026-07-05 — PDF : PDFKit retenu (pile unique, pure JS, zéro Chromium, wrapping texte natif, embed de fonts).
- 2026-07-05 — Schéma initial : 38 tables (budget ≤60). Champs de pilier en JSON versionné (PillarVersion) plutôt qu'une table par champ — l'UI lit/écrit le pilier entier, la certitude est par champ dans le JSON.
- 2026-07-05 — Scoring : composite /200 = somme des 8 piliers scorés /25 chacun ; barème par champ = complétude structurelle × multiplicateur de certitude (INFERRED 0.6, DECLARED 0.9, OFFICIAL 1.0), pondérations fixes en code, zéro LLM (verrouillé par test).
- 2026-07-05 — Templates de notifications/emails définis en code (typés), listés en Console avec envoi de test ; pas de table de templates éditables (sobriété — le cahier ne demande pas d'éditeur).
- 2026-07-05 — Chiffrement vault credentials : AES-256-GCM, clé dérivée HKDF de NEXTAUTH_SECRET (surcharge possible via VAULT_SECRET) — zéro dépendance, secrets système restent en env.
- 2026-07-05 — Branche `main` créée sur GitHub via l'API (base du commit Phase 0) : le repo distant était né sans branche par défaut, une PR exige une base. Draft PR #1 ouverte.
- 2026-07-05 — CI rouge au 1er push (typecheck) : leçon — relancer typecheck localement avant CHAQUE commit, pas seulement au scaffold. Augmentation de types JWT corrigée vers `@auth/core/jwt` (le module façade `next-auth/jwt` ne fusionne pas avec l'identité réelle des callbacks).
- 2026-07-05 — @playwright/test pinné ~1.56.0 : c'est la version appariée au chromium-1194 préinstallé dans l'environnement de dev distant ; la CI télécharge son navigateur et reste alignée.
- 2026-07-05 — Pas de page /inscription : l'entrée canonique d'un compte founder est le diagnostic (activation) ; la page connexion pointe vers /diagnostic. Talents/agences s'inscrivent via la Guilde (S7).
- 2026-07-05 — Témoignages de la landing rédigés depuis l'univers de démo seedé (Awa Cissé/Nyama Café, Moussa Diop/Guilde) — cohérents avec les données produit, à remplacer par de vrais verbatims client par l'opérateur.
- 2026-07-05 — JALON PHASE 1 : funnel complet E2E vert (landing → intake → résultat scoré → paywall → activation → cockpit) + connexion démo + mobile. 3/3 Playwright.
- 2026-07-05 — S2 : contenu légal FR rédigé de zéro (v1 absent) — structure opposable réelle, faits société marqués [À COMPLÉTER] jamais inventés ; à faire relire par l'opérateur/un conseil.
- 2026-07-05 — Blog en fichiers markdown (content/blog) rendus par `marked` (dep légère justifiée) : zéro table, zéro CMS ; l'opérateur publie en commitant.
- 2026-07-05 — Contact sans formulaire : WhatsApp (canal roi, wa.me depuis env) + email — pas de table ContactMessage, sobriété.
- 2026-07-05 — Mur public Guilde + fiches mission livrés en avance (S7) : les liens de navigation existaient et les données seedées les rendent réels ; dépôt/inscriptions/candidatures restent en S7.
- 2026-07-05 — Shells Console/Creator/Agency créés : les rôles seedés atterrissaient sur des 404 post-login. Console protégée par MFA TOTP effectif dès maintenant (enrôlement /mfa autonome + refresh de session unstable_update).
- 2026-07-05 — /realisations : uniquement des marques réellement pilotées dans l'outil (score + progression depuis 1er snapshot) — pas de mur de logos inventé.
- 2026-07-05 — S3 : Ma marque livré (édition champ par champ + validation INFERRED→OFFICIAL + versions + sources CRUD + RTIS refresh). Cockpit mono-marque en v2.0 (première marque non-shell du founder) — le multi-marques founder attendra un besoin réel ; le portefeuille opérateur (Console S6) couvre le multi.
- 2026-07-05 — Les modes d'amendement assistés LLM (reformulation/réécriture avec preview) arriveront avec la gateway LLM (tranche X.llm) — le manual-first est complet dès maintenant, mêmes endpoints.
- 2026-07-05 — S4 : fonts basculées WOFF2→OTF/TTF (fontkit plantait au subsetting WOFF2 à l'embed PDF : RangeError DataView) — un seul format sert web (next/font) et PDF (PDFKit). Vérifié accents français dans le PDF.
- 2026-07-05 — Oracle : modèle de blocs unique (p/list/kv/table/callout/score/empty) rendu à l'identique web et PDF ; bloc `empty` = honest-empty matérialisé ; chaque section trace ses sources (« le rapport ne dit que le déclaré »).
- 2026-07-05 — Sections 25 (BCG) et 26 (NPS) assument l'INSUFFISANT : matrices non calculables sans données de ventes/enquête — elles livrent le protocole de collecte au lieu de cases remplies au jugé.
- 2026-07-05 — S5 : règle d'or implémentée partout — un paiement n'est réglé (settle) QUE sur confirmation authentifiée : signature Stripe sur corps brut, re-lecture du statut via l'API du provider (Wave/MoMo/CinetPay/PayPal), ou validation humaine (manuel). Idempotence par WebhookEvent unique.
- 2026-07-05 — Provider MOCK réservé dev/E2E (PAYMENT_MOCK_ENABLED + NODE_ENV!==production, échec bruyant sinon) — c'est le « paiement test » des parcours d'argent.
- 2026-07-05 — Achat Oracle possible dès l'intake (avant compte) : le paiement est rattaché au token puis transféré à la marque à l'activation.
- 2026-07-05 — Décision produit : COCKPIT_MONTHLY gate l'intelligence/forge/MCP (récurrent) ; l'Oracle est un one-shot par marque, inclus dans les retainers — pas dans le Cockpit seul (c'est l'upsell).
- 2026-07-05 — Demande opérateur reçue en cours de mission : déploiement final sur Coolify (coolify.powerupgraders.com). Credentials stockés hors repo (scratchpad, chmod 600). Le contrat de portabilité §11.1 reste inchangé — zéro code Coolify dans l'app ; le déploiement se fera par l'API Coolify en fin de mission.
- 2026-07-05 — Note E2E : Intl fr-FR insère U+202F (espace fine insécable) dans les montants — assertions par chaînes (normalisées par Playwright), jamais par regex avec espace simple.
- 2026-07-05 — CI corrigée : (1) le provider MOCK est gaté par flag explicite + verrou anti-prod sur l'URL publique (la CI exécute un build production sur localhost — NODE_ENV seul était faux) ; (2) la CI sert désormais le VRAI artefact standalone (node .next/standalone/server.js + statiques copiées), ce qui valide l'artefact à chaque run.
- 2026-07-05 — S6 : l'édition opérateur des piliers passe par les mêmes pages et le même point d'écriture unique que le founder (?marque= + contrôle d'accès getOwnedBrand) — zéro double implémentation.
- 2026-07-05 — Playwright expect.timeout 15s : la compilation à la volée du dev server rendait les assertions 5s flaky ; la CI précompilée n'est pas concernée.
- 2026-07-05 — S7 : l'acceptation d'une candidature refuse proprement les autres candidatures ouvertes de la mission (décision opérateur unique) et crée le relevé de commission au taux du tier talent (config).
- 2026-07-05 — CI : public/ n'existait pas dans le checkout (dossier vide non tracké) — robots.txt réel + favicon (icon.svg App Router) ajoutés, étape de copie standalone rendue tolérante.
- 2026-07-06 — Les emails transactionnels du cycle Guilde (décision de modération au déposant sans compte) partiront avec la cascade email S8 — noté, pas de façade.
- 2026-07-06 — Banc E2E réformé : `scripts/e2e-full.sh` = base droppée/re-migrée/re-seedée + build standalone servi tel quel (PW_PROD=1) — 16/16 en ~31 s là où le dev server (recompilations en vol tuant les flux SSE, DB polluée par les runs) devenait progressivement flaky en 6 min. Le dev server reste pour l'itération sur un spec isolé.
- 2026-07-06 — SSE : sonde authentifiée directe = flux stable 12 s+ ; les fermetures précoces observées venaient exclusivement du HMR dev. Reprise ?since= rejouée depuis la base = aucune perte pendant les reconnexions.
- 2026-07-06 — Suppression de compte RGPD par anonymisation (email supprime-*@anonyme.invalid, PII nulles, profils/notifications purgés, marques détachées) : les factures restent conformes à l'obligation de conservation sans lien nominatif.
- 2026-07-06 — Réglages : digest opt-out (colonne User.digestOptOut) et push VAPID opt-in ; sans clés VAPID le bloc push affiche DEFERRED_AWAITING_CREDENTIALS.
- 2026-07-06 — S9 : cultIndex/devotion/radar extraits en module pur `intelligence/measures.ts` partagé avec l'Oracle s31 (implémentation unique) — le radar porte un état PAR AXE (OK/DEGRADED/INSUFFICIENT_DATA) + `basis` de traçabilité, valeur null si rien de calculable (jamais fabriquée).
- 2026-07-06 — S9 : chaque mutation communautaire (ajout/échelon/retrait) historise un BrandSnapshot (cultIndex, devotion, followers) — la trajectoire compte plus que le niveau, et elle est gratuite à ce point d'écriture.
- 2026-07-06 — S9 forge : 6 kinds réellement exposés dans l'UI (pas 139 « tools ») sous l'interface unique forgeAsset (entrée typée, sortie Zod). Les manques du socle deviennent des trous explicites « [À compléter : champ X du pilier Y] » — testé unitaire + E2E.
- 2026-07-06 — S9 : à l'activation d'un asset, si l'ADVE a bougé depuis sa composition, il naît déjà Périmé (staleAt = date du pilier le plus récent) — cohérent avec amendPillar qui périme les ACTIFS.
- 2026-07-06 — Piège Playwright (récidive) : getByText matche en SOUS-CHAÎNE (le marketing du gate contenait « Devotion Ladder ») et matche AUSSI le contenu des textarea (double match avec la vue lecture) — assertions d'absence sur des chaînes exclusives à l'état déverrouillé, .first() quand une textarea reflète le texte.
- 2026-07-06 — Couplage inter-specs assumé (workers:1, ordre alphabétique) : console.spec remplit ENGAGEMENT.rituels de Nyama → l'axe Visibilité passe OK. L'assertion radar cible Différenciation (champ `differenciation` qu'aucun spec ne touche) — déterministe en suite complète comme en isolé.
- 2026-07-06 — Seed : + gratuit@demo.test (founder sans abonnement ni marque) pour démontrer/tester les gates premium fermés avec upgrade path.
